import styled from "styled-components";

const BucketWrapper = styled.div`
    width: 100%;
    height: 100vh;
    color: #fff;

`;

const Buckets = () => {

    return(
        <BucketWrapper>
            본문
        </BucketWrapper>
    )
}

export default Buckets;